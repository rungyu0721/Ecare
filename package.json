{
  "name": "ecare-app",
  "version": "1.0.0",
  "private": true,
  "description": "E-CARE Capacitor app wrapper",
  "scripts": {
    "cap:prepare": "node scripts/prepare-capacitor-web.js",
    "cap:sync": "npm run cap:prepare && npx cap sync",
    "cap:copy": "npm run cap:prepare && npx cap copy",
    "cap:open:android": "npx cap open android",
    "cap:add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.1",
    "@capacitor/core": "^7.6.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.1"
  }
}
